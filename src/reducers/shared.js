import { GET_INITIAL_USERS} from '../actionTypes.js'

const shared =  (state = {}, action) => {
    let returnvalue;
        switch (action.type){       
           
            default :
                return state;
    }
}

export default shared;